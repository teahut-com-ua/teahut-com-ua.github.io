---
import { Icon } from "astro-icon/components";
import MainLayout from "../layouts/MainLayout.astro";

const pageTitle = "404 | Сторінку не знайдено";
---

<MainLayout>
    <section class="container">
        <h1 class="number">404</h1>
        <h3 class="centered">Ви вступили в Потік,</h3>
        <h3 class="centered">Але він вже не той, що раніше.</h3>
        <h3 class="centered last-line">Так і цяя сторінка спливла разом з ним...</h3>
        <a href="/" class="last-line">
            <button class="home-btn">
                Вертайтесь домів
                <Icon name="hut" size={27} />
            </button>
        </a>
    </section>
</MainLayout>

<style>
    .container {
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    .centered {
        text-align: center;
    }

    .home-btn {
        display: flex;
        justify-content: center;
        column-gap: 1rem;
        align-items: center;
        padding: 0.5rem 1rem 0.6rem;
        width: fit-content;
        border-radius: .6rem;
        cursor: pointer;
        font-size: 1rem;
        color: light-dark(var(--foreground), var(--green));
        background-color: light-dark(var(--button), var(--gray));
    }

    .number {
        font-size: 6rem;
        margin-bottom: 2rem;
    }

    .home-btn:hover {
        color: light-dark(var(--active),var(--background));
    }
</style>